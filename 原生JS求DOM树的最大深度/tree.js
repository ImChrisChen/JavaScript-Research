/** * Created by WebStorm. * User: chrischen * Date: 2020/7/15 * Time: 8:58 下午 */const getDepth = node => {    if (!node.children || node.children.length === 0) {        return 1    }    const maxChildrenDepth = [...node.children].map(v => getDepth(v))    return 1 + Math.max(...maxChildrenDepth)}console.log(getDepth(document.documentElement))function loop(tree) {    if (!(tree.children && tree.children > 0)) {        return 0;    }    return loop.children.map(item => loop(item));}